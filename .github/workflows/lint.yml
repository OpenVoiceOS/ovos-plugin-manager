name: Lint
on:
  push:
    branches: [master, dev]
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  lint:
    uses: OpenVoiceOS/gh-automations/.github/workflows/lint.yml@dev
    secrets: inherit
